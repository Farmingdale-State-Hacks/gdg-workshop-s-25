# Server configuration
server.port=8000

# API configuration - this is the PHP backend
api.base.url=http://localhost:8001/api

# Thymeleaf configuration
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML

# Logging
logging.level.com.detectivecase=DEBUG
logging.level.org.springframework.web=INFO

# Application info
app.name=DetectiveCase
app.description=Crime Pattern Explorer Using Google Cloud
app.version=1.0.0 